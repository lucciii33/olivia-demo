# Copy to .env and change these before deploying.
API_KEY=demo-api-key-123
BEARER_TOKEN=demo-bearer-token-abc

# MCP -> API connection
API_BASE=http://localhost:8000
# Path for the SQLite file (optional)
# DB_PATH=./demo.db
